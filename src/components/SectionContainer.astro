---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full mx-auto mb-10 container lg:max-w-4xl md:max-w-2xl`}>
  <slot />
</section>
